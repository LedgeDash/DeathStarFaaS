import sys, os, json
from bson.json_util import dumps
import pymongo

def handle(req):
    """handle a request to the function
    Args:
        req (str): request body
    """

    return sys.exit({"status": "ComposeAndPostTesting"})
